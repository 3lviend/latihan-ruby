<% if itemrepair.errors.any? %>
 <div id="error_explanation">
  <h2>
  <%= pluralize(itemrepair.errors.count, "Error") %> prohibited this data from being saved:
  </h2>
      <ul>
        <% itemrepair.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for itemrepair, url: url, method: meth do |f| %>
    <%= f.label :brand %>
    <%= f.text_field :brand %>
    <br>
    <%= f.label :type_hp %>
    <%= f.text_field :type_hp %>
    <br>
    <%= f.label :imei %>
    <%= f.text_field :imei %>
    <br>
    <%= f.label :damage %>
    <%= f.text_area :damage %>
    <br>
    <%= f.label :description %>
    <%= f.text_area :description %>
    <br>
    <%=f.hidden_field :customer_id, value: @customer.id %> 
    <%=f.hidden_field :staff_id, value: current_staff.id %> 
    <%= f.submit "Save" %>
    <%= link_to '<button type="button">Cancel</button>'.html_safe, roles_path %>
<% end %>        